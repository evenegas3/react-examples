import React from 'react'
// import PropTypes from 'prop-types'

function Modal2016(props) {

    return (
      <div>
          <p>Modal containing all of {props.year} data {props.year}</p>
      </div>
    );
  }

export default Modal2016;
